export const galleryHome = [
    {
      // tslint:disable-next-line: max-line-length
      url: `storage-facility-building`,
      // tslint:disable-next-line:max-line-length
      thumb: `storage-facility-building-thumb`,
      alt: `self storage building with white metal roll up doors under a blue sky`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-security-cameras`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-security-cameras-thumb`,
      alt: `security cameras on an electric pole facing in several directions`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-security-gate-pad`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-security-gate-pad-thumb`,
      alt: `a security pin-pad that controls access through an electronic gate`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-security-vehicle`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-security-vehicle-thumb`,
      alt: `the back of a professional security vehicle that performs regular security checks`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-unit-lock`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-unit-lock-thumb`,
      alt: `a cylinder lock on a self storage unit preventing access to the unit`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-units`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-units-thumb`,
      alt: `a row of self storage units with white metal closed doors`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `self-storage-units-row`,
      // tslint:disable-next-line:max-line-length
      thumb: `self-storage-units-row-thumb`,
      alt: `a row of white metal self storage unit doors all closed`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `storage-unit-row`,
      // tslint:disable-next-line:max-line-length
      thumb: `storage-unit-row-thumb`,
      alt: ` self storage unit doors closed and secured with bright red locks`
    },
    {
      // tslint:disable-next-line: max-line-length
      url: `storage-units`,
      // tslint:disable-next-line:max-line-length
      thumb: `storage-units-thumb`,
      alt: `two storage unit doors with locks securing the units closed`
    }
    
  ];
  